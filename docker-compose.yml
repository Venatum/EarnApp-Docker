version: "3.3"
services:
  app:
    build:
      context: build
      dockerfile: debian/Dockerfile
    image: venatum/earnapp:debian
    privileged: true
    cgroup_parent: ""
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
      - ./etc:/etc/earnapp
